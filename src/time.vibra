capability:
  $capability.clock: null
now-unix-millis:
  $function:
    capability: $capability
  return: $uint64
  do:
  - $wasm:
      import:
        module: vibra_v1
        name: clock_now_unix_millis
      args:
      - $args.capability
