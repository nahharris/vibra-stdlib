security:
  $import: ./security.vibra

now-unix-millis:
  $function: $security.clock-grant
  return: $uint64
  do:
    - $wasm:
        import:
          module: wasi_snapshot_preview1
          name: clock_time_get
        args:
          - $args.subject
