Fd:
  $union:
    variants:
      FromInt: $int

Path:
  $union:
    variants:
      FromStr: $str

File:
  $union:
    variants:
      FromPath: $Path

Dir:
  $union:
    variants:
      FromPath: $Path
